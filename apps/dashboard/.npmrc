# Ensure optional dependencies are installed (needed for lightningcss native modules)
# This is critical for lightningcss to work on Vercel's Linux build environment
# npm installs optional dependencies by default, but this ensures they're included
